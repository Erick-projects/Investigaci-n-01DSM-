<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/editTextTask"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ingrese una tarea"
        android:minHeight="48dp" />

    <Button
        android:id="@+id/buttonAddTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Agregar"
        android:layout_below="@id/editTextTask"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="8dp" />

    <EditText
        android:id="@+id/editTextSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Buscar tarea"
        android:layout_below="@id/buttonAddTask"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/buttonShowAllTasks"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="↩️"
        android:layout_below="@id/editTextSearch"
        android:layout_alignParentEnd="false"
        android:layout_alignParentStart="true"
        android:layout_marginTop="8dp" />

    <Button
        android:id="@+id/buttonSearchTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔍"
        android:layout_below="@id/editTextSearch"
        android:layout_toEndOf="@id/buttonShowAllTasks"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp" />

    <ListView
        android:id="@+id/listViewTasks"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonSearchTask"
        android:layout_marginTop="16dp"/>
</RelativeLayout>
